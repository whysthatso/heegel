<section class="eleven columns stitches content">
  <% if notice %>
    <p id="notice"><%= notice %></p>
  <% end %>
<h1>New product</h1>

<%= render 'form' %>

<%= link_to 'Back', products_path %>
</section>
